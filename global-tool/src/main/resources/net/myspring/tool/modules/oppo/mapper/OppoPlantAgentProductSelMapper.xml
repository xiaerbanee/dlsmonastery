<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.myspring.tool.modules.oppo.mapper.OppoPlantAgentProductSelMapper">

    <select id="findItemNumbers" resultType="String">
        select t.item_number from oppo_plant_agent_product_sel t where t.item_number in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <insert id="save">
        INSERT INTO oppo_plant_agent_product_sel
        (
            brand_id,
            brand_name,
            type_id,
            type_name,
            color_id,
            color_name,
            code_record_type,
            brand_type,
            item_desc,
            item_number,
            product_id,
            lx_product_id
        )VALUES
        <foreach item="item" index="index" collection="list" open="" separator="," close="">
            (
            #{item.brandId},
            #{item.brandName},
            #{item.typeId},
            #{item.typeName},
            #{item.colorId},
            #{item.colorName},
            #{item.codeRecordType},
            #{item.brandType},
            #{item.itemDesc},
            #{item.itemNumber},
            #{item.productId},
            #{item.lxProductId}
            )
        </foreach>
    </insert>

    <select id="findAll" resultType="OppoPlantAgentProductSel">
        select * from oppo_plant_agent_product_sel
    </select>

    <select id="findOne" resultType="OppoPlantAgentProductSel">
        select * from oppo_plant_agent_product_sel where id = #{id}
    </select>


</mapper>