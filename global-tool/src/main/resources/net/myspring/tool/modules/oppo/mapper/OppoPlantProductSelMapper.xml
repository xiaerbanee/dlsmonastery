<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.myspring.tool.modules.oppo.mapper.OppoPlantProductSelMapper">

    <select id="findColorIds" resultType="String">
        select t.color_id from oppo_plant_product_sel t where t.color_id in
        <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <insert id="save">
      INSERT  into oppo_plant_product_sel
      (
        color_id,
        brand_id,
        brand_name,
        color_name,
        remark,
        short_cut,
        type_id,
        type_name
      )VALUES
        <foreach item="item" index="index" collection="list" open="" separator="," close="">
            (
            #{item.colorId},
            #{item.brandId},
            #{item.brandName},
            #{item.colorName},
            #{item.remark},
            #{item.shortCut},
            #{item.typeId},
            #{item.typeName}
            )
        </foreach>
    </insert>
</mapper>