server:
  port: 1000
eureka:
  client:
     register-with-eureka: false
     fetch-registry: false
     serviceUrl:
       defaultZone: http://localhost:1000/eureka/

logging:
  file: summer-discovery.log

spring:
  application:
    name: summer-discovery