<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.myspring.uaa.mapper.AccountMapper">
    <select id="findByLoginName" resultType="Account">
        SELECT
        t1.*,
        t2.id as 'employee.id',
        t2.leave_date as 'employee.leaveDate'
        FROM
        hr_account t1,
        hr_employee t2
        WHERE
        t1.employee_id = t2.id
        and t1.login_name=#{loginName}
    </select>

</mapper>
