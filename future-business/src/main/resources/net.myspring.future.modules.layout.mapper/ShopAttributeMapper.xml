<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.myspring.future.modules.layout.mapper.ShopAttributeMapper">
    <select id="findByShopId" resultType="ShopAttribute">
        select  t1.*
        from crm_shop_attribute t1
        where t1.shop_id = #{shopId}
        and t1.enabled=1
    </select>

</mapper>
