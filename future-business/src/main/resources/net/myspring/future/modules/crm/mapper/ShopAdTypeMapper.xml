<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.myspring.future.modules.crm.mapper.ShopAdTypeMapper">

  <select id="findAllByEnabled" resultType="ShopAdType">
      SELECT t1.*
      FROM  crm_shop_ad_type t1
      where t1.enabled=1
  </select>

    <select id="findPage" resultType="ShopAdType">
        SELECT t1.*
        FROM crm_shop_ad_type t1
        where t1.enabled=1
        <if test="p.name!=null">
            and t1.name like concat('%',#{p.name},'%')
        </if>
        <if test="p.totalPriceType!=null">
            and t1.total_price_type=#{p.totalPriceType}
        </if>
    </select>

    <select id="findLabels" resultType="ShopAdType">
        SELECT t1.id,t1.name
        FROM crm_shop_ad_type t1
        WHERE t1.enabled=1
        and t1.id in
        <foreach collection="list" index="index" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

</mapper>
