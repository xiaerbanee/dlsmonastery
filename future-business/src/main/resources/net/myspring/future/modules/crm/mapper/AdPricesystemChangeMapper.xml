<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.myspring.future.modules.crm.mapper.AdPricesystemChangeMapper">

    <select id="findPage" resultType="AdPricesystemChange">
        SELECT
        t1.*
        FROM
        crm_ad_pricesystem_change t1
        WHERE
        t1.enabled=1
        <include refid="filter"></include>
    </select>
    <select id="findFilter" resultType="AdPricesystemChange">
        SELECT
        t1.*
        FROM
        crm_ad_pricesystem_change t1
        WHERE
        t1.enabled=1
        <include refid="filter"></include>
    </select>
    <sql id="filter">
        <if test="p.productName != null">
        AND t1.product_id in(
            select t2.id
            from crm_product t2
            where t2.name like CONCAT('%',#{p.pruductName},'%')
            )
        </if>
        <if test="p.productCode != null">
        AND t1.product_id in(
            select t2.id
            from crm_product t2
            where t2.code like CONCAT('%',#{p.productCode},'%')
            )
        </if>
    </sql>
</mapper>
