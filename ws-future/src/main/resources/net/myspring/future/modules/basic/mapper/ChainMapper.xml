<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.myspring.future.modules.basic.mapper.ChainMapper">

    <select id="findPage" resultType="ChainDto">
        SELECT
        t1.*
        FROM
        crm_chain t1
        WHERE
        t1.enabled=1
        <if test="p.name != null">
            AND t1.name LIKE CONCAT('%',#{p.name},'%')
        </if>
    </select>

    <select id="findDepotIds" resultType="String">
      select id from crm_depot where chain_id=#{id}
    </select>

</mapper>
