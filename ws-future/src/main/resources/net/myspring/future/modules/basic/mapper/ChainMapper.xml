<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.myspring.future.modules.basic.mapper.ChainMapper">

    <select id="findPage" resultType="ChainDto">
        SELECT
        t1.*
        FROM
        crm_chain t1
        WHERE
        t1.enabled=1
        <if test="p.name != null">
            AND t1.name LIKE CONCAT('%',#{p.name},'%')
        </if>
    </select>

    <select id="findLabels" resultType="Chain">
        SELECT t1.id,t1.name
        FROM crm_chain t1
        WHERE t1.enabled = 1
        and t1.id in
        <foreach collection="list" index="index" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <delete id="deleteByChainId">
        DELETE FROM crm_account_chain where chain_id = #{chainId}
    </delete>

    <insert id="saveAccountAndChain">
        INSERT INTO crm_account_chain(account_id,chain_id) VALUES
        <foreach collection="accountIdList" index="index" item="item" open="" separator="," close="">
            (#{item},#{chainId})
        </foreach>
    </insert>

</mapper>
