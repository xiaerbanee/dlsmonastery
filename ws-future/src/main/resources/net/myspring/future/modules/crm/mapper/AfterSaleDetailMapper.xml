<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.myspring.future.modules.crm.mapper.AfterSaleDetailMapper">

    <select id="findByAfterSaleListAndType" resultType="AfterSaleDetail">
        SELECT t1.*
        FROM crm_after_sale_detail t1
        WHERE t1.enabled=1
        and t1.type=#{type}
        and t2.after_sale_id in
        <foreach collection="list" index="index" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>
</mapper>
