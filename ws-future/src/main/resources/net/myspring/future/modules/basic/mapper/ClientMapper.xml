<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.myspring.future.modules.basic.mapper.ClientMapper">

    <select id="findPage" resultType="ClientDto">
        select t1.*
        from crm_client t1
        where
        t1.enabled=1
        <if test="p.name !=null">
            AND t1.name like concat('%',#{p.name},'%')
        </if>
    </select>

    <select id="findByDepotId" resultType="Client">
        select t1.*
        from crm_client t1, crm_depot t2
        where
        t1.enabled=1
        and t1.id = t2.client_id
        and t2.id = #{depotId}

    </select>




</mapper>
