<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.myspring.future.modules.basic.mapper.AdPricesystemMapper">

    <select id="findPage" resultType="AdPricesystemDto">
        SELECT
        t1.*
        FROM
        crm_ad_pricesystem t1
        WHERE
        t1.enabled=1
        <if test="p.name != null">
            AND t1.name LIKE CONCAT('%',#{p.name},'%')
        </if>

    </select>

    <select id="findList" resultType="AdPricesystem">
        SELECT
        t1.*
        FROM
        crm_ad_pricesystem t1
        WHERE
        t1.enabled=1
    </select>

    <select id="findByName" resultType="AdPricesystem">
        SELECT t1.*
        from crm_ad_pricesystem t1
        where t1.enabled=1
        and t1.name=#{name}
    </select>

</mapper>
