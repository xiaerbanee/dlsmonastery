<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.myspring.future.modules.basic.mapper.AdPricesystemMapper">

    <select id="findPage" resultType="AdPricesystemDto">
        SELECT
        t1.*
        FROM
        crm_ad_pricesystem t1
        WHERE
        t1.enabled=1
        <if test="p.name != null">
            AND t1.name LIKE CONCAT('%',#{p.name},'%')
        </if>

    </select>

    <select id="findList" resultType="AdPricesystem">
        SELECT
        t1.*
        FROM
        crm_ad_pricesystem t1
        WHERE
        t1.enabled=1
    </select>

    <select id="findByName" resultType="AdPricesystem">
        SELECT t1.*
        from crm_ad_pricesystem t1
        where t1.enabled=1
        and t1.name=#{name}
    </select>


    <delete id="deleteOfficeIds">
        DELETE FROM crm_ad_pricesystem_office where ad_pricesystem_id = #{adPricesystemId}
    </delete>

    <insert id="saveOfficeIds">
        INSERT INTO crm_ad_pricesystem_office
        (
          ad_pricesystem_id,office_id
        )VALUES
        <foreach item="item" index="index" collection="officeIds" open="" separator="," close="">
            (
            #{adPricesystemId},
            #{item}
            )
        </foreach>
    </insert>

</mapper>
