<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.myspring.future.modules.layout.mapper.AdPricesystemChangeMapper">

    <select id="findPage" resultType="AdPricesystemChangeDto">
        SELECT
        t1.*
        FROM
        crm_ad_pricesystem_change t1
        WHERE
        t1.enabled=1
        <if test="p.productId != null">
             AND t1.product_id = #{p.productId}
        </if>
    </select>
    <select id="findFilter" resultType="AdPricesystemChange">
        SELECT
        t1.*
        FROM
        crm_ad_pricesystem_change t1
        WHERE
        t1.enabled=1
        <include refid="filter"></include>
    </select>
    <sql id="filter">
        <if test="p.productId != null">
        AND t1.product_id = #{p.productId}
        </if>
    </sql>
</mapper>
