<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.myspring.basic.modules.sys.mapper.BackendModuleMapper">

    <select id="findPage"  resultType="BackendModuleDto">
        SELECT  t1.*
        FROM sys_backend_module  t1
        where t1.enabled=1
        <if test="p.name!=null">
            and t1.name =#{p.name}
        </if>
        <if test="p.backendName!=null">
            and t1.backend_id in (
                select id
                from sys_backend
                where t1.name like concat('%',#{p.backendName},'%')
            )
        </if>
    </select>

    <select id="findByBackendIds" resultType="BackendModule">
        SELECT t1.*
        FROM  sys_backend_module  t1
        where t1.enabled=1
        and t1.backend_id IN
        <foreach collection="list" index="index" item="item" open="("  separator="," close=")">
            #{item}
        </foreach>
    </select>

    <select id="findByPositionId" resultType="BackendModule">
        SELECT t1.*
         FROM  sys_backend_module  t1,hr_position_module t2
          where t1.enabled=1
          and t2.backend_module_id=t1.id
          and t2.position_id=#{positionId}
    </select>

</mapper>
