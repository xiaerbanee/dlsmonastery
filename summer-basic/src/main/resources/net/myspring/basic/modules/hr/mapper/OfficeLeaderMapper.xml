<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.myspring.basic.modules.hr.mapper.OfficeLeaderMapper">

    <update id="setEnabledByOfficeId">
        UPDATE  hr_office_leader set enabled=#{enabled} where office_id=#{officeId}
    </update>

    <update id="setEnabledByLeaderIds">
        UPDATE  hr_office_leader set enabled=#{enabled} where leader_id in
        <foreach collection="leaderList" index="index" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </update>

    <select id="findByOfficeId" resultType="OfficeLeader">
        SELECT t1.*
        FROM hr_office_leader t1
        where t1.enabled=1
        and t1.office_id=#{officeId}
    </select>

    <select id="findAllByOfficeId" resultType="OfficeLeader">
        SELECT t1.*
        FROM hr_office_leader t1
        where t1.enabled=1
        and t1.office_id=#{officeId}
    </select>

</mapper>
