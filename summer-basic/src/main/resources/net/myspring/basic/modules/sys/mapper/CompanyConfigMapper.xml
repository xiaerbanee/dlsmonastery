<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.myspring.basic.modules.sys.mapper.CompanyConfigMapper">

    <select id="findPage" resultType="CompanyConfigDto">
        SELECT
        t1.*
        FROM
        sys_company_config t1
        WHERE
        t1.enabled=1
        <if test="p.name != null">
            AND t1.name LIKE CONCAT('%',#{p.name},'%')
        </if>
        <if test="p.code != null">
            AND t1.code LIKE CONCAT('%',#{p.code},'%')
        </if>
    </select>

    <select id="findByCode" resultType="CompanyConfig">
        SELECT t1.*
        FROM sys_company_config t1
        where t1.enabled=1
        and t1.code=#{code}
    </select>


</mapper>
