<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.myspring.basic.modules.hr.mapper.AccountMessageMapper">

     <select id="findByAccountId" resultType="AccountMessage">
         SELECT t1.*
         FROM hr_account_message t1
         where t1.enabled=1
         and t1.to_account_id=#{accountId}
         and t1.created_date >=#{createdDateStart}
         and t1.readed = 0
     </select>

</mapper>
