<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.myspring.basic.modules.sys.mapper.ProcessTypeMapper">

    <select id="findByName" resultType="ProcessType">
        SELECT t1.*
        FROM sys_process_type t1
        where t1.name=#{name}
    </select>

    <select id="findEnabledAuditFileType" resultType="ProcessType">
         SELECT t1.*
        FROM sys_process_type t1
        WHERE t1.audit_file_type=1
        AND t1.enabled=1
    </select>

    <select id="findPage" resultType="ProcessType">
        SELECT
        t1.*
        FROM
        sys_process_type t1
        WHERE
        t1.enabled=1
        <if test="p.name != null">
            AND t1.name LIKE CONCAT('%',#{p.name},'%')
        </if>

    </select>

    <select id="findPositionNames" resultType="String">
        SELECT
            t1.`name`
        FROM
            hr_position_permission t,
            hr_position t1
        WHERE
        t.permission_id = #{permissionId}
        and t.position_id = t1.id
    </select>
</mapper>
